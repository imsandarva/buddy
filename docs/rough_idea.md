The idea is similar, but not exactly same, to smth like this: {{ https://www.heyclicky.com }},

 you can know what's that app about by searching it. And my idea is something liek this roughly : {{ i waas thinking if i could build smth like that, but for android phone. it doesn't need to be complex like heyclicky. just a buddy/cursor that can live on your screen and it can help people. cause you know lots of people, specially generation x or millenianls, they  can't even go to some phone's settings and change date & time, etc. i thought probably i can make a simple project that can press button on the phone or teach people saying "click here" do this do that, etc, through a cursor on the screen(so that it'd be easier for people to follow a cursor). etc. are you getting what i'm saying here ?   }} .



Where this is now: overlay cursor + accessibility eyes + hands + type. Voice is **audio-only** Gemini Live (`gemini-3.1-flash-live-preview`) — mic PCM in, voice PCM out, same cursor tools. Multi-step jobs (`run_goal`) hand off to a chat loop that keeps seeing and tapping on the screen in front of you. Eyes skip Buddy chrome and follow the home screen or app in front of you (not a frozen first snapshot). We do not share the screen as Live video; Gemini’s video input is still ≤1 fps JPEGs, so the SCREEN list is still the accessibility tree. Typed asks still use the chat API + on-screen list. See `docs/live.md`, `docs/goal.md`, `docs/ask.md`, `docs/type.md`, and `docs/brain.md`.
 


 Now the goal here is just not to point to things and say "here is the red button", "here is the blue button". it's to perform tasks, multi step tasks that requires seeing --> thinking little bit --> taking action --> seeing result again --> thinking --> action --> so on --> until the goal is achieved. 
 eg: i might just say: "hey, please tell me how  can I see how much storage is available on my phone". now it should see the screen(where is it currently) --> think how it can go to that storage viewing thing (probably settings button, probably notifiation panel or others ) --> take action --> again see the result --> think --> take action --> result --> so on --> until it reaches a screen where i can see the available storage on my phone. 

 you got what i'm trying to say here? and what i'm trying to make here. This kind of AI should be able to perform complex tasks, multi step tasks, and tasks doesn't limit to "how much storage is available on my phone" thing. yeah, while doing this kind of tasks, reasoning and thinking is needed, but not very complex thinking. we're not solving math problem here. little thinking, just enough to know or guess or have a sense of what action to take next by looking at the screen to acheive a goal.

 And for this, think what kind of tools might be needed, what kind of hands might be needed? should we only allow the buddycursor to move and tap on the screen? or more? like hold, and drag as well, or more? like basically there should be way or tools for buddycursor to do things that a human can do on their screen with their fingers you know. only then an AI or buddy would have enough flexible hands or way to perform lots of tasks.